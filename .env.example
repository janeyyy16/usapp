# Public deployed URL of this app (used to build links sent to other people,
# e.g. the "sign this document" link in Team Messenger). Without this, those
# links would use whatever host happens to be serving the page at send time —
# localhost while developing. Set to your real Cloudflare domain once deployed.
VITE_APP_URL=https://your-app.pages.dev

# Google Maps API Key
VITE_GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here

# Geoapify API Key (geocoding when the Ticket Map / other maps are toggled to Leaflet+OSM)
# Free tier: https://myprojects.geoapify.com/
VITE_GEOAPIFY_API_KEY=your_geoapify_api_key_here

# Firebase Configuration (Authentication + Storage)
# Get these from Firebase Console: https://console.firebase.google.com/
# Project Settings > General > Your apps > Web app config
VITE_FIREBASE_API_KEY=your_firebase_api_key_here
VITE_FIREBASE_AUTH_DOMAIN=your_firebase_auth_domain_here
VITE_FIREBASE_PROJECT_ID=your_firebase_project_id_here
VITE_FIREBASE_STORAGE_BUCKET=your_firebase_storage_bucket_here
VITE_FIREBASE_MESSAGING_SENDER_ID=your_firebase_messaging_sender_id_here
VITE_FIREBASE_APP_ID=your_firebase_app_id_here
VITE_FIREBASE_MEASUREMENT_ID=your_firebase_measurement_id_here

# Supabase Configuration (Business Data - NOT READY YET)
# Get these from Supabase Dashboard: https://app.supabase.com/
# Project Settings > API > Project URL and anon/public key
VITE_SUPABASE_URL=your_supabase_project_url_here
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Jotform webhook (SERVER ONLY - no VITE_ prefix, never exposed to browser)
JOTFORM_WEBHOOK_SECRET=your_jotform_webhook_secret_here
JOTFORM_TARGET_COMPANY_ID=your_target_company_uuid_here

# Jotform API key (SERVER ONLY) - required to download uploaded files from
# submissions, since Jotform's upload URLs are private by default (a plain
# fetch gets redirected to Jotform's login page instead of the file).
# Get it from: Jotform > Account Settings > API > Create New Key
JOTFORM_API_KEY=your_jotform_api_key_here

# Firebase service account (SERVER ONLY) used to write notifications via the
# Firestore REST API from the Jotform webhook, since the Firebase client SDK
# does not run reliably on Cloudflare Workers. Create one at:
# Firebase Console > Project Settings > Service Accounts > Generate new private key
# Copy "client_email" as-is, and "private_key" with its \n sequences kept literal.
FIREBASE_SERVICE_ACCOUNT_EMAIL=your_service_account_email_here
FIREBASE_SERVICE_ACCOUNT_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYOUR_KEY_HERE\n-----END PRIVATE KEY-----\n"

# Supabase service role key (SERVER ONLY - never exposed to browser). The
# Jotform webhook uses this to read `profiles` (role + extra_roles) directly,
# bypassing RLS, to find which users are HR (primary role or sub-role).
# Get it from: Supabase Dashboard > Project Settings > API > service_role key
SUPABASE_SERVICE_KEY=your_supabase_service_role_key_here
